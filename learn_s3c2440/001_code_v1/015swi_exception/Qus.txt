Q1、为什么在每一种MODE中，sp设置为不同的值？
A1: 如果设为一样的值,进入中断后,如果用到栈,会将主调程序的栈破坏

Q2: 在004中, 用	1) ldr pc, = main	main执行完, 无法返回主程序
				2) ldr lr, =halt
				   ldr pc, =main    main执行完返回主程序
				3) bl main 			main执行完返回主程序
	但是, 查看反汇编, 以上3种方式都有lr压栈和出栈的语句, 为什么会不一样?